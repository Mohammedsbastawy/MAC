# دليل استخدام Git و GitHub مع مشروع Atlas Control Panel

هذا الدليل يوفر خطوات واضحة لإدارة الكود الخاص بمشروعك باستخدام `Git` (نظام التحكم في الإصدارات) و `GitHub` (منصة لاستضافة مستودعات Git). سيساعدك هذا على حفظ تاريخ تغييراتك، والتعاون مع الآخرين، ونقل مشروعك بسهولة.

---

## 1. الإعداد لأول مرة: ربط المشروع بـ GitHub

اتبع هذه الخطوات إذا كان لديك المشروع على جهازك وتريد رفعه إلى GitHub لأول مرة.

### المتطلبات:
- **تثبيت Git:** تأكد من أن `Git` مثبت على جهازك. يمكنك تحميله من [git-scm.com](https://git-scm.com/download/win).

### الخطوات:

1.  **إنشاء مستودع جديد على GitHub:**
    *   اذهب إلى [GitHub.com](https://github.com) وقم بتسجيل الدخول.
    *   اضغط على علامة `+` في الزاوية العلوية اليمنى واختر `New repository`.
    *   أعطِ المستودع اسمًا (مثلاً: `atlas-control-panel`).
    *   اختر `Private` إذا كنت لا تريد أن يكون الكود عامًا.
    *   **مهم:** لا تقم بتحديد أي من الخيارات مثل `Add a README file` أو `.gitignore` أو `license` لأن مشروعك يحتوي عليها بالفعل.
    *   اضغط `Create repository`.

2.  **تهيئة Git في مجلد مشروعك:**
    *   افتح نافذة الأوامر (Command Prompt أو PowerShell) في المجلد الرئيسي لمشروعك.
    *   قم بتشغيل الأمر التالي لتهيئة Git:
        ```bash
        git init -b main
        ```

3.  **إضافة جميع الملفات للمتابعة:**
    *   قم بتشغيل الأمر التالي لإضافة كل الملفات والمجلدات في مشروعك إلى Git:
        ```bash
        git add .
        ```

4.  **حفظ التغييرات (Commit):**
    *   الآن، قم بحفظ هذه الإضافة الأولية مع رسالة توضيحية:
        ```bash
        git commit -m "Initial commit of Atlas Control Panel project"
        ```

5.  **ربط المستودع المحلي بالبعيد على GitHub:**
    *   في صفحة المستودع على GitHub التي أنشأتها، ستجد قسمًا بعنوان `…or push an existing repository from the command line`.
    *   انسخ الأمر الذي يبدأ بـ `git remote add origin`. سيكون شبيهًا بهذا:
        ```bash
        git remote add origin https://github.com/YourUsername/YourRepositoryName.git
        ```
    *   قم بلصق هذا الأمر في نافذة الأوامر وتشغيله.

6.  **رفع الكود إلى GitHub:**
    *   أخيرًا، قم برفع الكود من جهازك إلى GitHub:
        ```bash
        git push -u origin main
        ```

الآن مشروعك بالكامل موجود على GitHub!

---

## 2. سير العمل اليومي: حفظ ورفع التغييرات

بعد أن تقوم أنت أو الذكاء الاصطناعي بإجراء تعديلات على الكود، اتبع هذه الخطوات لحفظها.

1.  **إضافة التغييرات:**
    *   افتح نافذة الأوامر في مجلد المشروع.
    *   لإضافة كل الملفات التي تم تعديلها، استخدم الأمر:
        ```bash
        git add .
        ```

2.  **حفظ التغييرات (Commit) مع رسالة:**
    *   قم بحفظ التغييرات مع رسالة تصف ما قمت به. الرسائل الواضحة تساعدك في المستقبل.
        ```bash
        # مثال:
        git commit -m "Add user management features to Active Directory page"
        ```

3.  **رفع التغييرات إلى GitHub:**
    *   قم بمزامنة التغييرات المحفوظة مع المستودع على GitHub:
        ```bash
        git push
        ```
    *   إذا طلب منك تسجيل الدخول، أدخل اسم المستخدم وكلمة المرور أو استخدم `Personal Access Token`.

---

## 3. العمل على جهاز جديد: استنساخ المشروع

إذا أردت العمل على المشروع من جهاز كمبيوتر جديد، اتبع هذه الخطوات:

1.  **استنساخ المستودع (Clone):**
    *   اذهب إلى صفحة المستودع على GitHub.
    *   اضغط على الزر الأخضر `Code`.
    *   انسخ رابط الـ `HTTPS`.
    *   افتح نافذة الأوامر في المكان الذي تريد حفظ المشروع فيه (مثلاً، `D:\Projects`).
    *   قم بتشغيل أمر `git clone` مع الرابط الذي نسخته:
        ```bash
        git clone https://github.com/YourUsername/YourRepositoryName.git
        ```

2.  **الدخول إلى مجلد المشروع:**
    *   سيتم إنشاء مجلد جديد باسم المستودع. ادخل إليه:
        ```bash
        cd YourRepositoryName
        ```

3.  **تثبيت المكتبات:**
    *   الآن، اتبع نفس خطوات الإعداد الموجودة في ملف `README.md` لتثبيت كل ما يحتاجه المشروع ليعمل:
        *   تثبيت مكتبات الخادم الخلفي (Python):
            ```bash
            python -m pip install -r Tools/requirements.txt
            ```
        *   تثبيت مكتبات الواجهة الأمامية (Node.js):
            ```bash
            npm install
            ```

4.  **تشغيل التطبيق:**
    *   اتبع خطوات تشغيل الخادم والواجهة الأمامية كما هو موضح في `README.md`.

بهذه الطريقة، يمكنك التأكد من أن نسخة الكود لديك دائمًا محدّثة ومتزامنة عبر جميع أجهزتك.
